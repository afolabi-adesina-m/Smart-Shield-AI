# Ontario Smart-Shield capstone — ai_work_final environment
# Install base deps:
#   pip install -r requirements.txt
#
# Then install ONE of the PyTorch options:
#
#   # Option A — CUDA / GPU machines (DGX, Colab-style NVIDIA hosts)
#   pip install torch>=2.4 torchvision --index-url https://download.pytorch.org/whl/cu124
#
#   # Option B — CPU-only (laptops without NVIDIA GPU)
#   pip install torch>=2.4 torchvision --index-url https://download.pytorch.org/whl/cpu
#
# Then: Kernel -> Restart in the notebook
# The notebook setup cell also auto-chooses A vs B via an if statement.

pandas
numpy
matplotlib
seaborn
scipy
scikit-learn
pillow
packaging
lightgbm
imbalanced-learn
shap
joblib
torchvision
nbformat
nbclient

# Optional — only for seed_vision_cache.py (not needed for Run All)
# huggingface_hub
